# 安全策略

[English](SECURITY.md)

## 支持版本

安全修复只提供给最新发布的预发布版本。

| 版本 | 是否支持 |
| --- | --- |
| `0.1.0-alpha.1` | 是 |
| 更旧版本或未发布快照 | 否 |

## 报告漏洞

请使用仓库 Security 页签中的 **Report a vulnerability** 流程。不要在公开 issue、
discussion、pull request 或生成代码中披露疑似漏洞。

报告应包含最小复现、受影响版本或 commit、影响和建议缓解措施。请删除真实凭据、个人
数据和专有数据。维护者会在 72 小时内确认收到，并在七天内提供状态更新。修复和协调
披露时间取决于严重程度。

## 安全边界

范围内：

- cURL 解析或生成导致 shell 解释或文件访问。
- 生成代码中的注入或不安全转义。
- URL、请求头、正文、大小限制或稳定错误码校验绕过。
- 通过网络请求、存储、遥测、日志或 DOM 节点暴露输入。
- Workflow 权限、依赖、release 完整性或 artifact provenance 问题。

浏览器工具只解析和生成文本，不执行代码，也不发送所表示的 HTTP 请求。生成代码可能
按设计包含凭据；分享或执行前必须复核和脱敏。

范围外：

- 用户执行生成代码后的远程服务行为。
- 社会工程、物理访问或不支持的浏览器。
- 需要超过文档限制输入的可用性攻击。
- 没有项目特定利用方式或影响的依赖问题。

## Release 完整性

Release asset 包含 SHA-256 checksum、SPDX SBOM、许可证证据和 GitHub attestation。
使用下载 archive 前请完成验证。
